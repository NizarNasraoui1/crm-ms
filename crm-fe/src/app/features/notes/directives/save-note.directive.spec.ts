import { SaveNoteDirective } from './save-note.directive';

describe('SaveNoteDirective', () => {
  it('should create an instance', () => {
    const directive = new SaveNoteDirective();
    expect(directive).toBeTruthy();
  });
});
